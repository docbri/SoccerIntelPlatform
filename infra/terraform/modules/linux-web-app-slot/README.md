# Linux Web App Slot Module

## Purpose

Provision the `staging` deployment slot for the `Platform.Api` Azure Linux Web App.

## First Intended Use

- validate deployments before production promotion

## Responsibilities

- create the `staging` slot
- carry slot-specific configuration where needed

